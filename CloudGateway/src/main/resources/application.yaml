
server:
  port: 9090

spring:
  application:
    name: API-GATEWAY
  config:
    import: configserver:http://${CONFIG_SERVER_URL:localhost}:9296
  cloud:
    gateway:
      routes:
        - id : ORDER-SERVICE
          uri: lb://ORDER-SERVICE
          predicates:
            - Path=/order/**


        - id: PAYMENT-SERVICE
          uri: lb://PAYMENT-SERVICE
          predicates:
            - Path=/payment/**


        - id: PRODUCT-SERVICE
          uri: lb://PRODUCT-SERVICE
          predicates:
            - Path=/product/**

okta:
  oauth2:
    issuer: https://dev-32127018.okta.com/oauth2/default
    audience: api://default
    client-id: 0oa8s8w396iMb0eMd5d7
    client-secret: xzHmYQyPYzyUkUldG2WR2FdrGHD29xxJUP2cPclq
    scopes: openid, email, profile, offline_access
